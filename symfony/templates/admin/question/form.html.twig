<div class="container form-control w-75 my-4">
    {{ form_start(questionForm) }}
    {{ form_errors(questionForm) }}
    {{ form_row(questionForm.title) }}
    {{ form_row(questionForm.type) }}
    {{ form_row(questionForm.tickets) }}
    {{ form_row(questionForm.variant) }}
{#    <div>#}
{#        <span>Варианты</span>#}
{#        {% for variant in questionForm.vars.data.variant %}#}
{#            <div class="form-check">#}
{#                <input class="form-check-input" type="checkbox" value="" name="{{ variant.id }}" id="flexCheckDefault_{{ variant.id }}" {% if variant.id in questionForm.vars.data.answer %}checked {% endif %}>#}
{#                <label class="form-check-label" for="flexCheckDefault_{{ variant.id }}">#}
{#                    {{ variant.title }}#}
{#                </label>#}
{#            </div>#}
{#        {% endfor %}#}
{#    </div>#}

    <button class="btn btn-primary" type="submit">{{ buttonText }}</button>
{#    {{ form_rest(questionForm) }}#}

    {{ form_end(questionForm) }}
</div>

